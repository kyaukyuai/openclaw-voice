import contentLayoutStyles from './content-layout-styles';
import shellLayoutStyles from './shell-layout-styles';

const layoutStyles = {
  ...shellLayoutStyles,
  ...contentLayoutStyles,
};

export default layoutStyles;
